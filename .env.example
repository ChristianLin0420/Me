# ── Authentication ──
JWT_SECRET=change-this-to-a-random-string
ADMIN_PASSWORD=archivist2024

# ── Cloudflare R2 Storage (optional — falls back to local /uploads) ──
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=

# ── Database (optional — defaults to ./db/) ──
DB_DIR=

# ── Server ──
PORT=3000
NODE_ENV=development
